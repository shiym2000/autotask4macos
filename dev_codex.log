# AutoTask4macOS 开发日志

## 2026-04-16 10:31 - 远程 GPU 监控雏形

- 确定项目只在本地目录内开发，目标是监控远程服务器 GPU。
- 选择 Python 标准库实现后端，避免额外依赖。
- 选择 SSH config 原文作为服务器配置格式。
- 确定通过 `nvidia-smi -q -x` 获取 GPU 序号、名称、显存、使用率、功率、温度和进程。

## 2026-04-16 11:00 - 面向非技术用户的启动方式

- 将目标从命令行工具改为双击即可使用的 macOS 工具。
- 增加 `setup.command`、`start_monitor.command` 和后续的任务启动入口。
- 设计系统 Conda 环境 `autotask`，由 setup 自动创建或更新。
- 增加浏览器自动打开和网页首次配置向导。

## 2026-04-16 12:00 - 目录与安装流程整理

- 将主要代码放入 `src/`。
- 根目录保留用户最常接触的 setup/start 文档入口。
- 将早期运行文件放在 `src/`。
- setup 增加代理输入提示，方便网络环境受限时安装。

## 2026-04-16 13:00 - Monitor 页面迭代

- 显存单位从 MiB 改为 GB。
- 每张 GPU 改为一行显示。
- 服务器和 GPU 支持折叠，服务器默认折叠。
- 压缩行高，便于一次看更多条目。
- 进程列表改为 GPU 详情内直接显示。
- 增加进程数、PID、显存、进程名列。
- 增加 SSH 配置用户的进程高亮。

## 2026-04-16 14:00 - 浏览器生命周期与交互优化

- 增加网页心跳和关闭通知。
- 关闭网页时停止本地后台。
- 增加单击服务器折叠、双击服务器批量折叠 GPU。
- 将服务器/GPU/在线/刷新概览放入悬浮侧边栏。
- 删除自动刷新，改成打开时刷新一次，之后手动刷新。
- 刷新按钮增加转圈反馈。

## 2026-04-16 15:00 - Runner 任务功能

- 增加独立 Runner 页面和 `start_runner.command`。
- 复用同一份 SSH config。
- 支持远端创建 `tmux new-session -d` 并运行脚本。
- 增加服务器、GPU、任务名、远端脚本路径输入。
- 默认脚本路径设为 `~/projects/autotask.sh`。
- 增加任务列表、开始时间、运行时间、状态和 attach 命令复制。

## 2026-04-16 16:00 - 任务管理完善

- Monitor 和 Runner 分离为不同本地后台模式和端口。
- 两个页面的“编辑服务器”都改成居中弹窗。
- “添加任务”也改成居中弹窗。
- 添加远端 Conda 环境读取和选择。
- 增加“打开tmux”“终止程序”“删除tmux”“删除任务”。
- 破坏性操作前增加确认弹窗。
- 记录上一次添加任务时填写的内容。

## 2026-04-16 17:00 - SSH 压力排查与减载

- 排查 Monitor 和 Runner 同时访问同一服务器时的连接失败问题。
- 确认 Runner 打开时只读本地任务记录，不主动 SSH。
- 将任务状态刷新限制为显式点击刷新。
- 将 GPU 状态刷新限制为打开 Monitor 首次和用户手动刷新。
- 增加 `/api/status?refresh=1&host=<Host>`，Runner 只刷新当前服务器。
- SSH 调用增加 `ControlMaster=no` 和 `ControlPath=none`，避免影响用户其他 SSH 连接。

## 2026-04-16 18:00 - 表格与布局细化

- Monitor 进程表和 Runner 任务表支持点击列排序。
- 任务表支持拖拽列宽。
- 修正运行时间显示：不足一小时显示分钟和秒，超过一小时显示小数小时。
- 调整拖拽列宽逻辑，拖动一列边界时只影响相邻两列。
- 为每列设置最小宽度，避免拖出页面。

## 2026-04-16 19:00 - 命名与稳定性

- 用户可见项目名完成第一轮统一。
- Monitor 页面标题和 Runner 页面标题同步更新。
- setup/start 脚本提示同步更新。
- 本地后台 SSH 并发从 2 降到 1。
- 进程用户查询改为走统一 SSH 封装，共享超时、SSH 参数和限流。

## 2026-04-16 20:50 - GitHub 文档整理

- 新增面向 GitHub 的 README。
- 将开发文档转换为按时间线记录的开发日志。
- README 聚焦双击 setup、打开 Monitor、打开 Runner、SSH config 和排查。

## 2026-04-16 21:10 - AutoTask4macOS 文档与命名

- 项目名进一步明确为 AutoTask4macOS。
- README 改为简洁中文，并加入英文 README 链接。
- 新增 `README_en.md`。
- 开发日志改为中文。
- 项目目录名从 `autotask` 改为 `autotask4macos`。

## 2026-04-16 21:30 - Runner 操作列布局

- Runner 任务表操作列加宽，按钮区域改为 2x2 排列。
- 四个操作按钮略收窄，按从左到右、从上到下显示。
- README 和英文 README 同步说明两行紧凑操作按钮。

## 2026-04-16 21:45 - 本地数据目录与发布清理

- 将服务器配置、任务记录和运行日志统一迁移到 `data/`。
- 增加 `.gitignore`，默认不上传本地数据、日志和 Python 缓存。
- 首次运行会自动创建空的 `servers.conf`、`tasks.json` 和运行日志。
- README、英文 README 和页面占位示例改为脱敏示例。

## 2026-04-16 22:00 - 日志拆分与目录清理

- 将运行日志拆分为 `monitor.log` 和 `runner.log`。
- Monitor 和 Runner 关闭时会清空各自日志。
- setup 使用独立的 `setup.log`。
- 精简 `.gitignore` 并清理迁移后遗留文件。

## 2026-04-16 22:15 - README 面向用户精简

- README 和英文 README 改为面向普通使用者。
- 保留准备条件、快速开始、Monitor、Runner 和排查。
- 删除开发检查、目录结构和内部实现说明。

## 2026-04-16 22:30 - Runner 命名统一

- 将任务启动脚本改名为 `start_runner.command`。
- 将任务页面文件改名为 `src/runner.html`。
- 后端页面和应用模式改为 `runner`，页面地址改为 `/runner`。
- Runner 日志改为 `runner.log`。

## 2026-04-16 22:45 - Runner 完成状态识别

- 新任务启动时写入远端完成标记文件，记录退出码。
- 刷新任务状态时优先读取完成标记，避免 tmux 留存导致一直显示 running。
- 旧任务会兼容检测 tmux pane 中的完成提示并标记为 finished。

## 2026-04-16 23:00 - Runner 状态刷新误判修正

- 只有 tmux 明确报告 session 不存在时才标记 stopped。
- SSH 失败、超时或权限错误不再把 running 任务误改成 stopped。
- stopped 记录如果远端 session 仍存在，会在刷新时恢复为 running 或 finished。

## 2026-04-16 23:15 - Runner 状态名称简化

- 页面任务状态统一显示为 Running、Completed 和 Interrupted。
- 后端正常完成状态改为 completed。
- 启动失败、session 缺失、删除 tmux 和手动终止统一归为 interrupted。

## 2026-04-16 23:30 - Completed 状态保护

- 已正常完成的任务点击“终止程序”时不再改变状态。
- 已正常完成的任务点击“删除tmux”时只清理 tmux，不再改成 Interrupted。

## 2026-04-16 23:45 - 运行时间固定结束点

- 任务完成或中断时写入 `ended_at`。
- Runner 运行时间优先使用 `ended_at`，后续清理 tmux 不再拉长运行时间。

## 2026-04-17 00:00 - 打开 tmux 时自动 SSH

- Runner 点击“打开tmux”时继续复制 tmux attach 命令。
- 同时调用本地后端打开 macOS Terminal，并自动执行对应服务器的 `ssh <Host>`。

## 2026-04-17 00:15 - 完成时间戳驱动运行时间

- 新任务完成标记写入退出码和远端结束时间戳。
- Completed 任务运行时间基于完成标记中的结束时间戳计算。
- 移除旧任务 tmux pane 文本完成检测兼容逻辑。

## 2026-04-17 00:30 - Apache License

- 新增 Apache License 2.0 `LICENSE` 文件。
- README 和英文 README 补充 License 小节。
